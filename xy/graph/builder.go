package graph

import (
	"github.com/twpayne/go-geom"
	"github.com/twpayne/go-geom/xy/boundary"
)

func FromGeometry(boundaryRule boundary.NodeRule, geom geom.T) T {

}

// computeSelfNodes creates nodes for the graph based on where the geometry intersects itself
func computeSelfNodes(boundaryRule boundary.NodeRule, geom geom.T) {
	xy.In
}
